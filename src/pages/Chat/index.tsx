import React from 'react';

import SideBar from '../../components/SideBar';
import { Container } from './styles';

const Chat: React.FC = () => {
  return (
    <Container>
      <SideBar />
    </Container>
  );
};

export default Chat;
